from .common import *

DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.postgresql",
        "NAME": "samane_jame_test",
        "USER": "arman",
        "PASSWORD": "arman",
        "HOST": "postgres_test",
        "PORT": "5432",
    }
}
